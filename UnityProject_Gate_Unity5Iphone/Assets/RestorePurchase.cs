using UnityEngine;
using System;
using System.Collections;
using System.Collections.Generic;
using System.Linq;
using AssemblyCSharp;
using Assets.Scripts;
using UnityEngine;

public class RestorePurchase : MonoBehaviour { /*
    //public string levalName = "worldSelect";

     void OnMouseUp(){


        MyIAPManager[] allMyIAPManager = UnityEngine.Object.FindObjectsOfType<MyIAPManager>();
        //MyIAPManager[] allMyIAPManager = GameObject.FindObjectsOfType(typeof(MyIAPManager)) as MyIAPManager[];

        //GameObject[] allMyIAPManager = GameObject.. FindObjectWithTag("MyIAPManager");
        

        if (allMyIAPManager.Length == 0) {
            Debug.Log("RestorePurchase allMyIAPManager.Length == 0");
            return;
        }

        if (allMyIAPManager.Length > 1) {
            Debug.Log("RestorePurchase allMyIAPManager.Length > 1");
            return;
        }

        var manager = allMyIAPManager.FirstOrDefault();
        if (manager == null) {
            Debug.Log("Error RestorePurchase.OnMouseUp - manager is null");
            return;
        }
        manager.RestorePurchases();

        
        


    } */
}
