using UnityEngine;
using System.Collections;

public class AlgKiirus : MonoBehaviour {
	//Vector3 myVector = new Vector3 (1,0,0);
	void Update () {
	//	transform.position += transform.up * 2 * Time.deltaTime;
	//	transform.Translate (myVector * 2 * Time.deltaTime);
	}
}
